name: Build Windows Libraries

on:
  push:
    branches: [ lib_interface, main ]
  release:
    types: [ created ]

jobs:
  build-windows:
    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v4
      with:
        submodules: recursive
    
    - name: Setup MSYS2
      uses: msys2/setup-msys2@v2
      with:
        msystem: MINGW64
        install: >-
          mingw-w64-x86_64-gcc
          mingw-w64-x86_64-cmake
          mingw-w64-x86_64-ninja
          make
    
    - name: Build
      shell: msys2 {0}
      run: |
        # Fully build CaDiCaL library only (not mobical which needs sys/mman.h)
        cd libs/sat/cadical
        ./configure
        cd build
        make libcadical.a -j$(nproc)
        cd ../../../..
        
        # Fully build Intel SAT solver
        cd libs/intel_sat_solver
        make libr -j$(nproc)
        cd ../..
        
        # Touch the output files to prevent CMake from re-running custom targets
        touch libs/sat/cadical/build/makefile
        touch libs/sat/cadical/build/libcadical.a
        
        # Now run CMake build (dependencies already built)
        cmake -S . -B build -G "Unix Makefiles" -DCMAKE_BUILD_TYPE=Release -DCMAKE_POLICY_VERSION_MINIMUM=3.5
        cmake --build build --target allsat -j$(nproc)
    
    - name: Package
      shell: msys2 {0}
      run: |
        mkdir -p package
        cp -r include package/
        cp -r src package/
        cp build/liballsat.a package/
        cp libs/sat/cadical/build/libcadical.a package/
        cp libs/intel_sat_solver/libintel_sat_solver.a package/
        cp standalone_test/simple_test.cpp package/
        cp standalone_test/README.md package/
        tar -czf hall-windows-x64.tar.gz package/
    
    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: hall-windows-x64
        path: hall-windows-x64.tar.gz
